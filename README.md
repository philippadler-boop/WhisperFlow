# WhisperFlow

A tool that takes a given video and generates subtitles in a chosen language.

## Status

This is the pilot project for the [AI dev pipeline](../ai-dev-pipeline) —
the first project run end-to-end through the lifecycle described there
(Idea → Concept → Requirements → Architecture → Implementation → Test →
Review → Validation → Release → Maintenance).

- [x] Idea captured
- [ ] Concept brief
- [ ] Requirements spec
- [ ] Architecture / design
- [ ] Implementation
- [ ] First end-to-end release

See `docs/ideas/initial-idea.md` for the starting point.

## Next step

Open this folder in VS Code with the Claude Code extension and run GitHub
Spec Kit's `specify` phase against `docs/ideas/initial-idea.md` to turn this
into a proper concept + requirements spec, per the decision recorded in
`../ai-dev-pipeline/decisions.md` to use Spec Kit for this stage.
