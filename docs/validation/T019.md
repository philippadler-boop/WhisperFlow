# T019 Validation Report

PR: #106 (`issue-19-t019`)
Requirement: Unit tests for video probing and audio extraction edge cases, covering bad or unlaunchable input and missing-audio-track behavior.

## Requirement 1: Video probing handles an unlaunchable `ffprobe`

**Command:**

```text
python -m pytest tests/unit/test_video_probe.py::TestProbeVideoMocked::test_unlaunchable_ffprobe_raises_unsupported_format -vv
```

**Observed evidence:** The test passed. It creates an input `clip.mp4`, replaces `subprocess.run` with a `PermissionError` for `ffprobe`, and observes `UnsupportedVideoFormatError` with `exc_info.value.path == video_path`.

**Result:** PASS. The public error contract maps the unlaunchable probe to `UnsupportedVideoFormatError` and preserves the input path.

## Requirement 2: Audio extraction handles a missing audio track

**Command:**

```text
python -m pytest tests/unit/test_extract.py::TestExtractAudioMocked::test_no_audio_track_video_surfaces_as_extraction_error -vv
```

**Observed evidence:** The test passed. Mocked FFmpeg returns exit code 1 with `Output file does not contain any stream`, and the test observes `AudioExtractionError` whose message contains that diagnostic.

**Result:** PASS. The public error contract maps missing-audio extraction to `AudioExtractionError` while preserving the relevant FFmpeg diagnostic.

## Requirement 3: Owning test modules and regression coverage

**Command:**

```text
python -m pytest tests/unit/test_video_probe.py tests/unit/test_extract.py -q
```

**Observed evidence:** `50 passed in 0.51s`.

The implementation is validated against the repository's owning modules, `tests/unit/test_video_probe.py` and `tests/unit/test_extract.py`. The task text's `tests/unit/test_audio.py` path is stale and is not used as a failure criterion.

**Result:** PASS.

## Additional validation

**Commands and observed results:**

```text
python -m pytest tests/unit -q
235 passed in 3.65s

python -m pytest tests/integration/test_input_errors.py -q
2 passed in 0.56s

python -m ruff check .
All checks passed!
```

No tests were skipped and no environment limitations were observed. The PR remains open on `issue-19-t019`, targets `main`, and its body contains the required `Closes #19` line.

## Overall result

**PASS** - T019 requirements are independently validated on PR #106.
