# Validation Report: T004 — Implement domain error types in `src/lib/errors.py`

- **PR**: #65, "T004: Implement domain error types in `src/lib/errors.py` (#4)"
  (open at validation time, `004-domain-error-types` → `main`; CI green,
  reviewer-approved after one round of `CHANGES_REQUESTED` — a
  `MaxDurationExceededError` message-formatting mismatch with
  `contracts/cli.md` — fixed in a follow-up commit)
- **Branch validated**: `004-domain-error-types` @ commit
  `485706aa137f2c29b4d819f24cea86c08da0ca13` ("T004: fix max-duration
  formatting to match contracts/cli.md (PR #65 review)"), on top of
  `35d2011` ("Add domain error types for FR-007 input validation (#4)").
  Confirmed this commit hash matches the PR's `headRefOid` exactly via
  `gh pr view 65 --json headRefOid`.
- **Task source**: `specs/001-video-subtitle-generator/tasks.md`, line 38
  (marked `[x]` on this branch)
- **Validated by**: `qa` (independent re-check; CI status and reviewer
  approval not trusted as evidence — every claim below was exercised with a
  throwaway script or `pytest` run directly in this environment)
- **Validation date**: 2026-09-05
- **Method**: Read `contracts/cli.md` (exit-code/error-message contract)
  and `data-model.md` (Video validation rules) first, then read
  `src/lib/errors.py` and `tests/unit/test_errors.py` on the checked-out
  branch. Wrote a standalone Python script (outside the repo, in the
  session scratchpad — not committed) that imports `src/lib/errors.py` via
  the project's editable install and independently raises/catches all
  three error types, checks attributes, checks the exact contract string,
  probes two additional max-duration edge cases, and triggers the
  defensive `ValueError` boundary — then ran it directly with
  `python <script>.py` and inspected the real printed output. Separately
  ran `python -m pytest -q` and `python -m pytest -v tests/unit/test_errors.py`
  for the full-suite check. The script was deleted after use; the
  repository was left clean (verified via `git status` afterward).

## Requirement

> T004 Implement domain error types `UnsupportedVideoFormatError`,
> `MaxDurationExceededError`, `FfmpegNotFoundError` in `src/lib/errors.py`
> (spec FR-007; data-model.md validation rules)

Cross-checked against `contracts/cli.md` before validating: exit code `1`
covers "unsupported/corrupt video format, video exceeds the 2-hour
maximum, required `ffmpeg` binary not found," with the literal example
error message `"Error: video exceeds maximum supported length of 2 hours
(got 2:14:03)"`. `data-model.md`'s Video validation rules require
`container_format` to be one of the supported formats and
`duration_seconds` to be `≤ 7200s`, both routing to FR-007's "clear error"
requirement. This decomposes into the 6 checkable sub-requirements given
in the task prompt.

## Evidence

### 1. Import, raise/catch all three error types, confirm attributes

Ran a script that imports `lib.errors` (resolved via the project's
editable pip install — `whisperflow` shows
`Editable project location: C:\Users\phili\Projects\WhisperFlow`, with
`src` on `sys.path`), then does a real `raise`/`except` round-trip for
each type. Actual captured output:

```
Caught UnsupportedVideoFormatError: unsupported video format 'avi' for '\videos\clip.avi' (supported formats: mp4, mov, mkv)
  path attr: \videos\clip.avi <class 'pathlib.WindowsPath'>
  detected_format attr: avi
Caught UnsupportedVideoFormatError (no format): could not determine a supported video format for '\videos\mystery.bin' (supported formats: mp4, mov, mkv)
Caught MaxDurationExceededError: video exceeds maximum supported length of 2 hours (got 2:13:20)
  duration_seconds: 8000.0
  max_duration_seconds: 7200.0
Caught FfmpegNotFoundError: required 'ffmpeg' binary was not found on PATH (install ffmpeg and ensure it is on PATH)
  executable: ffmpeg
Caught FfmpegNotFoundError (custom exe): required 'ffprobe' binary was not found on PATH (install ffmpeg and ensure it is on PATH)
```

All three types were genuinely raised and caught by their own exception
class (not just constructed). Attribute assertions
(`e.path == Path("/videos/clip.avi")`, `e.detected_format == "avi"`,
`e.detected_format is None` when omitted, `e.duration_seconds == 8000.0`,
`e.max_duration_seconds == MAX_DURATION_SECONDS`, `e.executable ==
"ffmpeg"`/`"ffprobe"`) all held — the script would have raised
`AssertionError` and stopped otherwise, and it ran to completion. **PASS.**

### 2. Exact-match contract string, `duration_seconds=2*3600+14*60+3`

Constructed `MaxDurationExceededError(duration_seconds=2*3600+14*60+3)`
directly and compared with `==` (not `in`) against the literal contract
string:

```
actual:   'video exceeds maximum supported length of 2 hours (got 2:14:03)'
expected: 'video exceeds maximum supported length of 2 hours (got 2:14:03)'
```

`assert actual == expected` passed — confirmed character-for-character
identical to `contracts/cli.md`'s example (excluding the `Error: ` prefix,
which the contract itself attributes to the future CLI layer, not this
exception). **PASS.**

### 3. Max-duration formatter edge cases (independently chosen values)

Ran four edge cases through the real formatter (two were the
task-specified minimum; two more added for extra confidence since this was
the exact area the reviewer flagged as under-tested):

| Max (s) | Actual (s) | Rendered message |
|---|---|---|
| 5400 (1h30m, non-whole-hour) | 6000 | `video exceeds maximum supported length of 1 hour 30 minutes (got 1:40:00)` |
| 45 (sub-minute) | 100 | `video exceeds maximum supported length of 45 seconds (got 0:01:40)` |
| 61 (1m1s) | 125 | `video exceeds maximum supported length of 1 minute 1 second (got 0:02:05)` |
| 120 (2 whole minutes) | 200 | `video exceeds maximum supported length of 2 minutes (got 0:03:20)` |

All four read as natural, grammatically correct English: correct
pluralization in every case (`1 hour`/`30 minutes`, `45 seconds`,
`1 minute`/`1 second` — note both singular components render correctly in
the same string, not just the previously-tested "1 hour 30 minutes" case),
and the actual/offending duration stays in precise `H:MM:SS` throughout,
matching the split the reviewer traced by inspection but had not seen
executed. **PASS** — no bugs found in the areas the reviewer explicitly
called out as untested (61s exercises singular-minute-and-singular-second
together, which neither the PR's suite nor the reviewer's manual trace
covered).

### 4. Defensive boundary check raises `ValueError`

Triggered both boundary conditions directly:

```
Equal case correctly raised ValueError: MaxDurationExceededError requires duration_seconds > max_duration_seconds (got duration_seconds=7200.0, max_duration_seconds=7200.0)
Less-than case correctly raised ValueError: MaxDurationExceededError requires duration_seconds > max_duration_seconds (got duration_seconds=100.0, max_duration_seconds=7200.0)
```

`duration_seconds == max_duration_seconds` (7200.0/7200.0) and
`duration_seconds < max_duration_seconds` (100.0/7200.0) both genuinely
raised `ValueError` with a message identifying both operands — matching
data-model.md's rule that only `duration_seconds > 7200s` is the invalid
(reportable) case, so equality must be rejected by the constructor rather
than silently accepted. **PASS.**

### 5. Common `WhisperFlowError` base is genuinely catchable

Wrote and ran an `except WhisperFlowError:` block against instances of all
three concrete types:

```
Caught UnsupportedVideoFormatError via WhisperFlowError base: unsupported video format 'avi' for 'clip.avi' (supported formats: mp4, mov, mkv)
Caught MaxDurationExceededError via WhisperFlowError base: video exceeds maximum supported length of 2 hours (got 2:46:40)
Caught FfmpegNotFoundError via WhisperFlowError base: required 'ffmpeg' binary was not found on PATH (install ffmpeg and ensure it is on PATH)
```

Each `raise` was caught by the `except WhisperFlowError` clause specifically
(not a bare `except Exception`), confirming the inheritance chain works at
runtime, not just per `issubclass()` checks in the PR's own tests.
**PASS.**

### 6. Full test suite

Environment: Python 3.12.10, pytest 9.0.2 (already installed; `whisperflow`
installed editable), `ffmpeg 8.0-essentials_build` present on `PATH`.

```
$ python -m pytest -q
.......................................                                  [100%]
39 passed in 0.23s
```

All 39 tests pass, with no `ffmpeg`-related failures in this environment
(unlike the PR description's noted sandbox gap, `ffmpeg` is present here,
so `tests/unit/test_conftest_fixtures.py` also passes rather than being
skipped/excused). Specifically for `test_errors.py`:

```
$ python -m pytest -v tests/unit/test_errors.py
...
tests/unit/test_errors.py::TestMaxDurationExceededError::test_message_matches_contract_example_exactly PASSED
...
20 passed in 0.04s
```

All 20 of the PR's own `test_errors.py` tests pass, including the
fix-commit's added `test_message_matches_contract_example_exactly` and
`test_custom_max_duration_handles_non_whole_hours`. **PASS.**

## Pass/Fail

| Sub-requirement | Result |
|---|---|
| All three error types importable, raisable, catchable by their own class, with correct attributes (`path`/`detected_format`, `duration_seconds`/`max_duration_seconds`, `executable`) | **PASS** |
| `MaxDurationExceededError(duration_seconds=2*3600+14*60+3)` → `str(err)` exactly equals `"video exceeds maximum supported length of 2 hours (got 2:14:03)"` (`==`, not substring) | **PASS** |
| Max-duration formatter produces sensible phrasing for a non-whole-hour max (5400s) and a sub-minute max (45s), plus two additional edge cases | **PASS** |
| `MaxDurationExceededError` raises `ValueError` when `duration_seconds <= max_duration_seconds` (both equal and less-than triggered) | **PASS** |
| All three error types are catchable via a common `WhisperFlowError` base, verified with a real `except WhisperFlowError:` block | **PASS** |
| Full test suite (`pytest -q`) passes | **PASS** (39 passed, 0 failed) |

**Overall: PASS.** T004 is fully satisfied on branch
`004-domain-error-types` at commit `485706a`. The exact contract string is
pinned character-for-character (not just in CI/the PR's own assertion —
independently reconstructed and compared here), the fix that closed the
reviewer's `CHANGES_REQUESTED` round genuinely resolved the mismatch, and
the specific gap the reviewer flagged as under-tested (max-duration
formatter edge cases beyond the whole-hour and single "1 hour 30 minutes"
case) was independently exercised here with four cases, including a
double-singular case (61s → "1 minute 1 second") not present anywhere in
the PR's own suite — no defects found.

## Environment cleanup

The verification script was written to and run from this session's
scratchpad directory (outside the repository), not committed to the
repo. No repository file other than this report was created or modified;
`git status` on `004-domain-error-types` was "nothing to commit, working
tree clean" both before and after this validation.
