# Validation Report: T012/T013/T014 - Integrated no-review subtitle pipeline

- **PR**: #97
- **Branch validated**: `14-wire-no-review-pipeline` @ `afbef3f`
- **Task source**: `specs/001-video-subtitle-generator/tasks.md` (T012/T013/T014)
- **Requirements source**: `spec.md` FR-003, FR-006, FR-007, FR-008, FR-011; `data-model.md`; `contracts/cli.md`; ADRs 0001-0004
- **Validated by**: QA, independently executed on 2026-09-06 against the checked-out commit, matching `origin/14-wire-no-review-pipeline`. Existing untracked `docs/usr_tasks/` content was left untouched.

## Requirement Evidence

### Transcript segments -> ordered standard SRT - FR-003, FR-006 / T012

Command:

```text
python -m pytest tests/unit/test_writer.py tests/unit/test_video_probe.py tests/unit/test_extract.py -k "ordered or compose or write or missing_ffprobe or unlaunchable or cleanup or failure" -q
```

Observed: `23 passed, 38 deselected in 0.31s`.

The writer tests exercise transcript-segment to `SubtitleLine`/`SubtitleFile` conversion, standard SRT composition, nested output paths, and write failures. The real clear-speech smoke test independently produced a standard SubRip block with a 1-based index, `HH:MM:SS,mmm` timestamps, and source-language text.

**PASS** - Transcript segments are converted into ordered subtitle lines and written as a standard `.srt` file.

### `--no-review` pipeline dispatch, progress, and model propagation - FR-011 / T013/T014

Commands:

```text
python -m pytest tests/unit/test_pipeline.py -k "model_load_error or transcription_error or write_subtitles_raises or cleanup_failure or no_audio_track or runs_all_stages or reports_progress" -q
python -m pytest tests/unit/test_cli_main.py -k "no_review or unsupported_format or oversized or missing_ffmpeg or subtitle_write or ffprobe_launch" -q
```

Observed: `8 passed, 6 deselected in 0.24s` and `7 passed, 17 deselected in 0.33s`.

These tests verify probe -> extract -> transcribe -> write ordering, the `model_size` argument reaching transcription, segment percentage progress, stage announcements, and `--no-review` forwarding the selected model size and output path.

Independent real-fixture command:

```text
$env:PYTHONPATH='src'; python -m cli.main transcribe tests/fixtures/clear_speech.mp4 --no-review --model tiny --output $env:TEMP\whisperflow-qa-clear.srt
```

Observed: `EXIT_CODE=0`, `OUTPUT_EXISTS=True`, `OUTPUT_BYTES=100`, with stage output for Extracting audio, Transcribing, `91.7%` progress, Writing subtitles, and Done. The generated SRT contained:

```text
1
00:00:00,000 --> 00:00:03,200
Hello, this is a test on the whisper flow sub-title generator.
```

PowerShell rendered the contract's Unicode ellipsis as a console encoding artifact (`à` in captured output); the stage labels and ordering were otherwise observed. Progress was separate from the output file.

**PASS** - `--no-review` dispatches through all required stages, reports ongoing progress, and propagates the selected model size.

### No detectable speech, including no audio track - FR-008 / T013

Independent real-fixture command:

```text
$env:PYTHONPATH='src'; python -m cli.main transcribe tests/fixtures/silence.mp4 --no-review --model tiny --output $env:TEMP\whisperflow-qa-srt
```

Observed: all stages and `No speech detected in the input video -- wrote an empty subtitle file.`, followed by `Done.`, `EXIT_CODE=0`, `OUTPUT_EXISTS=True`, and `OUTPUT_BYTES=0`.

A second independent command generated a one-second MP4 with `ffmpeg -f lavfi ... -an`, so it had no audio track. The CLI produced the same successful notice and stages, with `EXIT_CODE=0`, `OUTPUT_EXISTS=True`, and `OUTPUT_BYTES=0`. Focused pipeline tests also verify extraction/transcription are skipped for `Video.has_audio_track=False` while stages remain announced.

**PASS** - Silent input and a genuinely audio-less video succeed with a clear no-speech notice and an empty `.srt`.

### Domain errors and `SubtitleWriteError` - FR-007 / T014

Command:

```text
python -m pytest tests/unit/test_cli_main.py -k "unsupported_format or oversized or missing_ffmpeg or subtitle_write or ffprobe_launch" -q
```

Observed: `7 passed, 17 deselected in 0.33s`.

Coverage includes unsupported format, over-2-hour input, missing `ffmpeg`, model/transcription failure, subtitle-write failure through the new `SubtitleWriteError`, and a present-but-unlaunchable `ffprobe`. The CLI assertions require exit code `1`, one human-readable `Error:` line, and no raw `OSError`/traceback; writer tests verify the filesystem error is translated to `SubtitleWriteError` with its reason preserved.

Independent real unsupported-input command observed:

```text
Error: could not determine a supported video format for 'tests\\fixtures\\not-a-video.txt' (supported formats: mp4, mov, mkv)
EXIT_CODE=1
OUTPUT_EXISTS=False
```

**PASS** - Requested expected failures are surfaced as clear domain errors rather than raw tracebacks, including subtitle-write and ffprobe launch paths.

### Temporary audio cleanup and primary-error precedence - data-model / T013

Commands:

```text
python -m pytest tests/unit/test_pipeline.py -k "model_load_error or transcription_error or write_subtitles_raises or cleanup_failure" -q
python -m pytest tests/unit/test_writer.py tests/unit/test_video_probe.py tests/unit/test_extract.py -k "ordered or compose or write or missing_ffprobe or unlaunchable or cleanup or failure" -q
```

Observed: `8 passed, 6 deselected in 0.24s` and `23 passed, 38 deselected in 0.31s`.

The tests verify temporary audio removal after success, transcription/model failure, and subtitle-write failure. Forced cleanup errors become a domain error after an otherwise successful run, while cleanup failure during a primary transcription failure does not replace the primary `TranscriptionError`. Extraction tests cover cleanup of partial output on extraction failures. The real clear-speech, silent, and no-audio runs completed successfully without leaking pipeline-owned extracted audio.

**PASS** - Temporary audio cleanup occurs across success and failure paths, and cleanup errors do not mask a primary processing failure.

## Test and Lint Runs

Focused unit and contract tests:

```text
python -m pytest tests/unit/test_writer.py tests/unit/test_pipeline.py tests/unit/test_cli_main.py tests/unit/test_video_probe.py tests/unit/test_extract.py tests/unit/test_progress.py tests/contract/test_cli_contract.py -q
150 passed in 1.20s
```

Full suite:

```text
python -m pytest -q
253 passed in 3.57s
```

Ruff:

```text
ruff check src tests
All checks passed!
```

Runtime prerequisites observed: `ffmpeg.exe` at `C:\ProgramData\chocolatey\bin\ffmpeg.exe` and `ffprobe.exe` at `C:\ProgramData\chocolatey\bin\ffprobe.exe`.

The package was not installed under a `whisperflow` module name in this checkout, so real CLI smoke tests used the declared src-layout entry point with `PYTHONPATH=src` (`python -m cli.main`). This is an environment invocation detail, not a product failure.

## Validation Result

**T012/T013/T014 PASS.** All requested integrated requirements were independently observed at commit `afbef3f`. No application code or tests were changed by QA; exactly this validation report is the QA artifact added by this run.
