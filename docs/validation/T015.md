# Validation Report: T015 - Basic transcription integration test

- **PR**: #95
- **Branch validated**: `015-integration-test-transcribe-basic` @ `5b57d9e`
- **Task source**: `specs/001-video-subtitle-generator/tasks.md` (T015)
- **Scenario source**: `specs/001-video-subtitle-generator/quickstart.md` Scenario 1
- **Requirements source**: `specs/001-video-subtitle-generator/spec.md` FR-001, FR-002, FR-003, FR-006
- **Validated by**: QA, independently executed on 2026-09-06 against the exact requested head. No application code or tests were changed.

## Requirement Evidence

### Accept a video file and extract spoken audio - FR-001 / T015

Focused integration command:

```text
python -m pytest tests/integration/test_transcribe_basic.py -q
```

Observed:

```text
1 passed in 1.80s
```

The test copies the real `tests/fixtures/clear_speech.mp4` into an isolated temporary directory and invokes the actual Typer application with:

```text
whisperflow transcribe <copied fixture> --model tiny --no-review
```

No mocks or patched pipeline functions are used by the integration test. The real pipeline completed audio extraction and transcription, and the direct CLI run below independently reproduced the same behavior.

**PASS** - The CLI accepted the real video fixture and successfully processed its spoken audio.

### Produce a time-synchronized source-language transcript - FR-002 / T015

The focused test requires exactly one subtitle block, compares the complete normalized token sequence, and compares the complete timing sequence against the fixture's known real-transcription result.

Expected and observed token sequence:

```text
hello, this, is, a, test, on, the, whisper, flow, sub-title, generator
```

Expected and observed timing sequence:

```text
(0.0, 3.2)
```

The direct CLI run independently reported exit code `0` and generated:

```text
1
00:00:00,000 --> 00:00:03,200
Hello, this is a test on the whisper flow sub-title generator.
```

**PASS** - The complete spoken token sequence and complete subtitle timing sequence matched the required fixture evidence.

### Generate subtitles in the video's original language - FR-003 / T015

The real fixture's output text was the English source-language transcription shown above. The integration assertion extracts and compares every normalized token, including `sub-title`, and does not permit translated or incomplete output.

**PASS** - The generated subtitle text remained in the video's spoken language and matched the complete expected source-language sequence.

### Write a standard subtitle file - FR-006 / T015

Direct CLI command:

```text
whisperflow transcribe tests/fixtures/clear_speech.mp4 --model tiny --no-review --output <temporary path>/whisperflow-qa-T015-clear.srt
```

Observed:

```text
EXIT_CODE=0
OUTPUT_EXISTS=True
```

The output was a valid standard SubRip file with a 1-based block number, `HH:MM:SS,mmm` timestamps, and the expected subtitle text. The focused test parses the generated file with the `srt` parser and verifies its block count, content, and timings.

**PASS** - A standard `.srt` file was written at the requested output path with the expected content and timing.

## Test and Lint Runs

Full test suite:

```text
python -m pytest -q
254 passed in 4.44s
```

Ruff:

```text
ruff check src tests
All checks passed!
```

Runtime prerequisites were available: Python 3.12.10, `ffmpeg`, `ffprobe`, the installed `whisperflow` entry point, and the cached/downloadable `tiny` model needed by the real run.

On Windows, the captured console rendered the CLI's Unicode ellipsis in stage announcements as `à`; stage ordering (`Extracting audio`, `Transcribing`, `Writing subtitles`) and successful completion were still observable, and the generated SRT was unaffected. This is an environment display limitation, not a validation failure.

## Validation Result

**T015 PASS.** FR-001, FR-002, FR-003, and FR-006 were independently validated at exact head `5b57d9e`. The focused integration test passed against the real fixture and real pipeline, the direct CLI produced the expected complete text and timing output, the full pytest suite passed, and Ruff passed.
