# Validation Report: T016 - Progress feedback integration test

- **PR**: #103
- **Branch validated**: `copilot/t016-integration-test-progress-lines` @ `f0f9e95`
- **Base**: `origin/main` @ `e9d3fb2`
- **Task source**: `specs/001-video-subtitle-generator/tasks.md` (T015, T016)
- **Scenario source**: `specs/001-video-subtitle-generator/quickstart.md` Scenarios 1 and 2
- **Requirements source**: `specs/001-video-subtitle-generator/spec.md` FR-001, FR-002, FR-003, FR-006, FR-011; `specs/001-video-subtitle-generator/contracts/cli.md`
- **Validated by**: QA, independently rerun on 2026-09-06 against the exact requested head. No application code or tests were changed.

## Environment

Validation ran on Windows PowerShell with Python 3.12, the installed
`whisperflow` entry point, cached `faster-whisper` tiny model, `ffmpeg`,
`ffprobe`, and Ruff. The fixture was the repository's real
`tests/fixtures/clear_speech.mp4`; no transcription or pipeline functions
were mocked.

The Windows subprocess emitted stage ellipses as byte `0x85`, which is the
cp1252 representation of `…`. Decoding that stream as UTF-8 displays `�`,
but the raw bytes and the console rendering confirm the intended ellipsis.
The same raw capture contained `
` for the in-place percentage update.

## Requirement Evidence

### Accept video input and extract spoken audio - FR-001 / T015

Focused command:

```text
python -m pytest tests/integration/test_transcribe_basic.py -q
```

Observed:

```text
1 passed in 1.80s
```

The test copied the real clear-speech fixture to an isolated temporary
directory and invoked the actual CLI with `--model tiny --no-review`. The
real pipeline accepted the video, extracted audio, transcribed it, and wrote
the output file.

**PASS** - T015 independently verifies the real video-to-subtitle path for
FR-001.

### Produce a time-synchronized source-language transcript - FR-002 / T015

The focused T015 test observed exactly one subtitle block and asserted the
known fixture timing shape. The independent CLI smoke run produced:

```text
1
00:00:00,000 --> 00:00:03,200
Hello, this is a test on the whisper flow sub-title generator.
```

The stable text anchors `hello`, `test`, and `generator` were all present;
the block count was `1`; and the observed timestamp pair was
`00:00:00,000` to `00:00:03,200`.

**PASS** - The observed output is a time-coded transcript of the fixture's
spoken English, with stable-anchor assertions that tolerate tiny-model
lexical variation across runtimes.

### Generate subtitles in the video's original language - FR-003 / T015

The real smoke output was English, matching the fixture's spoken language.
The T015 assertion checks stable spoken words and does not accept a
translated or empty result.

**PASS** - The output remained in the source language.

### Write a standard subtitle file - FR-006 / T015

Independent CLI smoke command:

```text
whisperflow transcribe tests/fixtures/clear_speech.mp4 --model tiny --no-review --output <temporary path>/clear_speech.srt
```

Observed:

```text
EXIT_CODE=0
STDOUT_REPR=''
OUTPUT_EXISTS=True
BLOCK_COUNT=1
TIMESTAMPS=[('00:00:00,000', '00:00:03,200')]
```

The generated file contained a 1-based SubRip block with standard
`HH:MM:SS,mmm --> HH:MM:SS,mmm` timing and the observed spoken text.

**PASS** - A valid `.srt` file was written to the requested output path.

### Show ongoing progress on stderr - FR-011 / T016

Focused command:

```text
python -m pytest tests/integration/test_progress.py -q
```

Observed:

```text
1 passed in 1.76s
```

The integration test invoked the real Typer application and asserted exit
code `0`, empty stdout, the `Extracting audio…`, `Transcribing…`, and
`Writing subtitles…` stage announcements, plus an in-flight percentage
matching `\rTranscribing… <number>.<number>%`.

The independent subprocess smoke run observed exit code `0`, empty stdout,
and stderr equivalent to:

```text
Extracting audio…
Transcribing…
\rTranscribing…  91.7%
Writing subtitles…
Done.
```

The raw stderr bytes were:

```text
b'Extracting audio\\x85\\r\\nTranscribing\\x85\\r\\n\\rTranscribing\\x85  91.7%\\r\\nWriting subtitles\\x85\\r\\nDone.\\r\\n'
```

**PASS** - T016 and the independent CLI run both demonstrate stage
announcements and percentage progress on stderr while stdout remains clean.

## Test and Lint Runs

Full test suite:

```text
python -m pytest -q
255 passed in 5.36s
```

Ruff:

```text
ruff check src tests
All checks passed!
```

These are supporting quality checks, not substitutes for the direct
requirement evidence above.

## Validation Result

**T015 PASS. T016 PASS.** FR-001, FR-002, FR-003, FR-006, and FR-011 were
independently validated at exact head `f0f9e95`. The affected and focused
integration tests passed, the real CLI smoke run produced the expected
source-language SRT shape and timing with the required stderr progress, the
full pytest suite passed, and Ruff passed.
