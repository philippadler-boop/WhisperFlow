# Validation Report: T016 - Progress feedback integration test

- **PR**: #103
- **Branch validated**: `copilot/t016-integration-test-progress-lines` @ `9211b18`
- **Base**: `origin/main` @ `e9d3fb2`
- **Task source**: `specs/001-video-subtitle-generator/tasks.md` (T016)
- **Scenario source**: `specs/001-video-subtitle-generator/quickstart.md` Scenario 2
- **Requirements source**: `specs/001-video-subtitle-generator/spec.md` FR-011; `specs/001-video-subtitle-generator/contracts/cli.md` Progress contract
- **Validated by**: QA, independently executed on 2026-09-06 against the exact requested head. No application code or tests were changed.

## Requirement Evidence

### Show ongoing progress while processing - FR-011 / T016

Focused integration command:

```text
python -m pytest tests/integration/test_progress.py -q
```

Observed:

```text
1 passed in 1.81s
```

The integration test invokes the real Typer application with the real
`tests/fixtures/clear_speech.mp4` fixture, `--model tiny`, and
`--no-review`. It asserts exit code `0`, empty stdout, the three required
stage announcements, and an in-flight transcription percentage on stderr.

**PASS** - The focused T016 integration behavior passed against the real
fixture and pipeline.

### Keep progress on stderr and stdout empty - FR-011 / CLI progress contract

Independent real CLI invocation through `typer.testing.CliRunner`:

```text
python -c "... CliRunner().invoke(app, ['transcribe', 'tests/fixtures/clear_speech.mp4', '--model', 'tiny', '--no-review', '--output', '<temporary path>/t016.srt']) ..."
```

Observed:

```text
EXIT_CODE=0
STDOUT_REPR=''
STDERR_REPR='Extracting audio…\nTranscribing…\n\rTranscribing…  91.7%\nWriting subtitles…\nDone.\n'
OUTPUT_EXISTS=True
```

The captured stderr contains the required `Extracting audio…`,
`Transcribing…`, and `Writing subtitles…` stage lines plus a percentage
while transcribing. Stdout is exactly empty and the requested `.srt` file
was created. Because this run used the in-process CliRunner capture, it
also independently confirms the pipeline's default `sys.stderr` binding
is resolved at invocation time and reaches `result.stderr`.

**PASS** - The CLI output channels and progress stream behavior match the
contract, including the stderr capture fix.

## Test and Lint Runs

Full test suite:

```text
python -m pytest -q
255 passed in 5.07s
```

Ruff:

```text
ruff check src tests
All checks passed!
```

Runtime prerequisites were available: Python 3.12.10, ffmpeg 8.0, ffprobe
8.0, Ruff 0.16.6, the installed project entry point, and the cached tiny
model required by the real transcription run.

The environment is Windows PowerShell. The captured repr preserved the
Unicode ellipsis and carriage return, so no display ambiguity affected
this validation. No environment limitation prevented execution.

## Validation Result

**T016 PASS.** FR-011 and the CLI progress contract were independently
validated at exact head `9211b18`. The focused integration test passed,
the real Typer CLI produced the required stderr stage and percentage
progress with empty stdout under CliRunner, the output file was created,
the full pytest suite passed, and Ruff passed.
